# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /etc/make.conf.example for a more detailed example.
#CFLAGS="-O2 -march=prescott -pipe -fomit-frame-pointer"
CFLAGS="-march=prescott -O2 -pipe"
CXXFLAGS="${CFLAGS}"
# WARNING: Changing your CHOST is not something that should be done lightly.
# Please consult http://www.gentoo.org/doc/en/change-chost.xml before changing.
CHOST="i686-pc-linux-gnu"
USE="acpi gnome gtk ogg mp3 alsa -oss -cups -esd -arts -mad X mmx sse sse2 png x264 ssse3 unicode cdparanoia svg consolekit dbus hal avahi -arts -qt3 -qt4 mysql qt3support opengl smp jpeg truetype libnotify networkmanager laptop"
MAKEOPTS="-j5"
#MAKEOPTS="-j1" 
ACCEPT_KEYWORDS="x86 ~x86"
VIDEO_CARDS="intel nouveau"
LINGUAS="nb nb_NO"
INPUT_DEVICES="keyboard mouse evdev synaptics"
ACCEPT_LICENSE="*"
source /var/lib/layman/make.conf
DRACUT_MODULES="plymouth"
EMERGE_DEFAULT_OPTS="--autounmask=n"
I_WANT_GNOME_3_3_X=yes
